# p5js-geometry-playground

## Running

To run it simply open the `index.html` file in your browser. Alternatively run

```npx http-server . -c-1```

to get a web server running on `http://localhost:8080/`
